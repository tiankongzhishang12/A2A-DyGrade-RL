[project]
name = "a2a-dygrade-rl"
version = "0.1.0"
description = "A2A-DyGrade-RL offline research experiment pipeline"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
